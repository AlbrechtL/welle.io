import QtQuick 2.2
import QtQuick.Controls 2.2

// Import custom styles
import "."

ComboBox {
    id: control

    /*background: Rectangle {
            implicitWidth: 1200
            implicitHeight: 40
            border.color: control.pressed ? "#468bb7" : "#468bb7"
            border.width: control.visualFocus ? 2 : 1
            radius: 2
        }*/
}
