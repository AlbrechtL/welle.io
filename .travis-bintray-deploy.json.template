{
  "package": {
    "name": "welle.io_next_nighly", // Bintray package name
    "repo": "welle.io",         // Bintray repository name
    "subject": "albrechtl"      // Bintray subject (user or organization)
  },
  "version": {
    "name": "${VERSION}"
  },
  "files": [
    {
      "includePattern": ".(.*welle-io.*.AppImage)",
      "uploadPattern": "$1"
    }
  ],
  "publish": true
}

